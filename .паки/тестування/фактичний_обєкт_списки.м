взяти функційні_операції.містить_елемент як м_ел
взяти функційні_операції.еквівалентний_списку як екв_сп

дія _(ФактичнийОбєкт)
  дія ФактичнийОбєкт.має_довжину(очікувана_довжина число)
    я.значення = я.значення.довжина()

    якщо (!я.навпаки і (я.значення != очікувана_довжина))
      я.провалити_припущення(я, очікувана_довжина, "Очікується, що список має довжину %(очікувана_довжина)")
    якщо (я.навпаки і (я.значення == очікувана_довжина))
      я.провалити_припущення(я, очікувана_довжина, "Очікується, що список має довжину, відмінну від %(очікувана_довжина)")
    кінець
  кінець

  дія ФактичнийОбєкт.містить_елемент(елемент)
    я.значення = м_ел(я.значення, елемент)
    якщо (!я.навпаки і !я.значення)
      я.провалити_припущення(я, так, "Очікується, що список містить елемент %(елемент)")
    якщо (я.навпаки і я.значення)
      я.провалити_припущення(я, так, "Очікується, що список не містить елемент %(елемент)")
    кінець
  кінець

  дія ФактичнийОбєкт.еквівалентний_списку(очікуваний_список список)
    я.значення = екв_сп(я.значення, очікуваний_список)
    якщо(!я.навпаки і !я.значення)
      я.провалити_припущення(я, так, "Очікується, що список еквівалентний списку %(очікуваний_список)")
    якщо(я.навпаки і я.значення)
      я.провалити_припущення(я, так, "Очікується, що список не еквівалентний списку %(очікуваний_список)")
    кінець
  кінець
кінець

дати _